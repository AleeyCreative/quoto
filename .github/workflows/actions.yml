name: "Quoto Github Actions"
on: push
jobs:
    deploy-quoto:
        runs-on: ubuntu-latest
        steps:
            - name: "Checkout quoto-latest"
              uses: actions/checkout@v1
              with:
                  repository: "AleeyCreative/quoto"
                  ref: "master"
